variaveis podem começar com letra, $ ou _

não podem começar com números

é possível usar letras ou números

é possível usar acentos e símbolos

não podem ontas espaços

não podem ser palavras reservadas



Maiusculas e minusculas fazem diferença
tente escolher nomes coerentes para a variavel

tipos primitivos

number = numeros

string = caracteres

boolean = true or false

typeof + nomedavariavel, mostra o tipo da variavel

variaveis podem ser var ou let

cria a variavel e já atribui valor
var nome = "david"

atribui valor a variavel
nome = "uchiha"


Converter srting para numero

Number.parseInt(n) para inteiro
Number.parseFloat(n) para real
Number(n)

Numero para string

string(n)
n.toString()


nome = david
'O aluno ' + nome // concatenação
`O aluno ${nome}` // template string

Formatar strings

nome.lenght - me diz quantas letras tem
nome.toUppercase() - tudo para maiusculas
nome.toLowercase() - tudo para minusculas


Formatar numeros

n1 = 1545.5
n1.toFixed(2) = 1545.50
n1.toFixed(2).replace('.', ',') = 1545,50
n1.toLocaleString('pt-BR', {style: 'currency', currency: 'BRL'}) = R$ 1.545,50
n1.toLocaleString('pt-BR', {style: 'currency', currency: 'USD'}) = US$ 1.545,50


Operadores Aritméticos 

+ adição
- subtração
* multiplicação
/ divisão
% resto da divisão
** potência

Ordem de precedência

()
**
* / %
+ -

Atribuição simples

var a = 5 + 3 // = recebe   8
var b = a % 5 // 3
var c = 5 * b ** 2 // 45
var d = 10 - a / 2 // 6
var e = 6 * 2 / d // 2
var f = b % e + 4 / e // 3

Auto-Atribuição
var n = 3
n = n + 4  // 7 ou n += 4

Incremento

var x = 5
x = x + 1 // 6    ou x++
x = x - 1 // 5    ou x--

Relacionais

5 > 2 maior  // true
7 < 4 menor // false
8 >= 8 maior ou igual // true
9 <= 7 menor ou igual // false
5 == 5 igual // true
4 != 4 diferente // false

Operador de identidade restrita

5 == 5 // true
5 == "5" // true
5 === "5" // false
5 != 5 // false
5 !== "5" // true

Operadores Lógicos

! negação
&& conjunção
|| disjunção

true && true // true
true && false // false
false && true // false 
false && false // false

true || true // true
true || false // true
false || true // true
false || false // false

Exemplo:

a = 5
b = 8
a > b && b % 2 == 0  //  false
a <= b || b / 2 == 2   // true
Ordem de execução, aritméticos, relacionais, lógicos.

Ordem de execução, primeiro o não, e ou.

Ordem de precedência

() ** / ...
> < >= ...
!
&&
||

Operador ternário
? :

media >= 7.0 ? "Aprovado" : "Reprovado"

Exemplo 1
var x = 8
var res = x % 2 == 0 ? 5:9
x = 8 
res = 5

Exemplo 2
var idade = 19
var r = idade >=18 ? "MAIOR":"MENOR"
r = maior

watch in chrome - extensão
node exec - extensão - f8 para rodar javascript pelo node

DOM - document object model

window >> location - document - history
document >> html
html >> head - body
head >> meta - title
body >> h1, p...etc

Selecionar Elementos

por marca - getElementByTagName()
por id - getElementById()
por Nome - getElementsByName()
por Classe - getElementsByClassName()
por Seletor - querySelector()  querySelectorAll()


Eventos DOM
mouseenter - chega o mouse dentro da div, entrou na div.
mousemove - movendo o mouse
mousedown - clique mouse mouseuop- soltou o click
click - clique completo do mouse
mouseup - quando o mouse sai da div

Funções

Conjunto de linhas que serão executadas mediante evento